[package]
name = "amri-windows"
version = "0.1.0"
edition = "2021"

[dependencies]
amri-core = { path = "../../crates/amri-core" }
amri-subscriptions = { path = "../../crates/amri-subscriptions" }
eframe = "0.32"
egui_extras = { version = "0.32", features = ["all_loaders"] }
image = { version = "0.25", default-features = false, features = ["png"] }
